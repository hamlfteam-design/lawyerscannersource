<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same ink navy as the app's own theme (Theme.kt's `Ink`), so the
         launcher icon reads as the same product as the UI behind it. -->
    <color name="ic_launcher_background">#1E2A38</color>
</resources>
